# T16 — Intent-aware overview and ranking

**Status:** Done

Implemented `cortex overview` and `context --overview`, deterministic module summaries, entry-point ranking, overview sections, and scaffold-memory filtering. Verified with `go test ./...` and live Cortex overview output.
