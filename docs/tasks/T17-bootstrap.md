# T17 — Deterministic bootstrap and memory validation

**Status:** Done

Implemented proposal-only `cortex bootstrap`, `--write` missing-memory generation, deterministic module/project/architecture/convention discovery, generated provenance frontmatter, and `cortex memory check`. No model or network dependency is used; existing manual memory is preserved.
