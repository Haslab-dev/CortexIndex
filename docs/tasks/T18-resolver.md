# T18 — Cortex binary discovery

**Status:** Done

Implemented bounded binary resolution for explicit `CORTEX_BIN`, PATH, project `dist/cortex`/`cortex`, user-local install, and safe shell fallback. Updated the Skill and generated AGENTS workflow without baking checkout paths into committed instructions.
