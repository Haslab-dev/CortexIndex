# T10 — README & docs

**Status:** Backlog
**Depends on:** T08
**PRD sections:** all (orientation)

## Goal

README that gets a new user (human or agent) productive in one minute.

## Scope

- What Cortex is / is not (§4 non-goals), architecture summary (§5), install/build.
- Quickstart: `cortex init → cortex index → cortex context "<task>"`.
- Command reference table (§16), config.md options, memory file guide (§7–§11, §18, §19).
- Skill installation, design principles (§31), performance targets (§25), V2 outlook (§29).
- Success-metric framing (§30).

## Acceptance criteria

- [ ] README covers build, quickstart, all commands, memory lifecycle, skill adoption.
- [ ] Consistent `cortex` / `.cortex/` naming throughout.
