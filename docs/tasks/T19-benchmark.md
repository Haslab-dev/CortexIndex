# T19 — Local deterministic benchmark

**Status:** Done

Implemented `cortex benchmark`/`make bench` with a deterministic fixture, baseline-vs-Cortex arms, preparation timing separated from task latency, logical calls/files/lines, chars/4 token estimates, success oracle, and Markdown/JSON output. Metrics are explicitly local approximations, not provider telemetry.
